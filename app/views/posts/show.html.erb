<h2><%= @post.title %></h2>
<div><%= @post.body %> </div>

<br><br>
<%= link_to "Edit Post", edit_post_path(@post)  %>
<%= link_to "Delete Post", post_path(@post), method: :delete, data: {confirm: "Are you sure?"}   %>
<%= link_to "Back to Post", posts_path(@post)  %>
<br><br>

